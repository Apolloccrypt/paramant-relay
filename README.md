# Review screenshots for PR #339

Playwright, Chromium, 390x844, device scale factor 2, no session. Taken on the
`feat/fe-product` branch at commit `db34521`, served from `frontend/` over a
local static server. These are review artefacts only; the branch holds no code
and is not meant to be merged.

| file | what it shows |
|---|---|
| `parasign-390-fold.jpg` | /parasign, first screen: h1, first paragraph, both buttons, and the split heading already visible |
| `parasign-sec1.jpg` | /parasign section 01, the split: free for everyone, then the business plans |
| `parasend-390-fold.jpg` | /parasend, first screen |
| `parasend-sec1.jpg` | /parasend section 01, the same split |
| `sign-390-fold.jpg` | /sign, first screen: the explanatory copy above the tool, and the CTA that used to render as plain text |
